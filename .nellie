{
    "commands": [
        "apt-get install -y libxml2-dev libxslt-dev zlib1g-dev",
        "bundle install --without integration",
        "bundle exec rake"
    ],
    "environment": {
        "CI": "true"
    }
}
